# URL base de la API. Déjala sin definir en desarrollo (usa el proxy de Vite → http://localhost:3000).
# En producción, pon la URL pública del backend, incluyendo /api:
# VITE_API_URL=https://api.tudominio.com/api
